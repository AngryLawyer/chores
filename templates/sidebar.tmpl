<!-- Sidebar -->
<div id="sidebar-wrapper">
    <ul class="sidebar-nav">
        <li class="sidebar-brand">
            <a href="/">
                Chores
            </a>
        </li>
        {% for page in pages %}
            {% if page.active %}
            <li class="active">
            {% else %}
            <li>
            {% endif %}
                <a href="{{page.url}}">{{page.label}}</a>
            </li>
        {% endfor %}
    </ul>
</div>
<!-- /#sidebar-wrapper -->
